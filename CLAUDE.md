# 프로젝트 목적

이 저장소는 **Java 개발자가 Kotlin을 학습하기 위한 문서/예제 저장소**다.
`docs/`에는 개념 설명 문서를, `src/main/kotlin/com/kotlin/`에는 그 문서에
대응하는 실행 가능한 예제 코드를 둔다.

---

## 규칙 1 — 근거는 반드시 Kotlin 공식 문서

- 문법, 동작 방식, API 시그니처 등 **모든 기술적 설명은 추정이나 기억에
  의존하지 않고 Kotlin 공식 문서(kotlinlang.org, Spring Framework 공식
  Kotlin 레퍼런스 등)를 실제로 조회하여 확인한 내용만 작성한다.**
- 확인이 애매하거나 공식 문서로 검증하지 못한 내용은 쓰지 않는다.
  차라리 "이 부분은 공식 문서에서 확인 필요"라고 표시하고 남겨둔다.
- 새 문서를 작성하거나 기존 문서를 수정할 때는 관련 공식 문서를 검색/조회한
  뒤 작성하고, 문서 하단에 참고한 URL을 **"참고 자료"** 절로 남긴다.
  (예: `docs/coroutine/basics.md`의 "참고 자료" 절 형식을 따른다.)
- 예제 코드는 작성 후 실제로 컴파일·실행해서 출력을 확인한다
  (설명과 실제 동작이 다르면 안 된다).

## 규칙 2 — 모든 예시는 "Java 예시 → Kotlin 예시" 순서로 구성

- 개념 하나를 설명할 때는 **항상 Java 코드 스니펫을 먼저 보여주고, 그 다음에
  대응하는 Kotlin 코드 스니펫을 보여준다.** 순서를 바꾸지 않는다.
- 두 코드는 같은 시나리오(같은 클래스/메서드/변수명)를 다뤄야 비교가 쉽다.
- Java 코드에 대한 설명(왜 이렇게 짜야 했는지, 어떤 보일러플레이트가
  필요한지)을 먼저 짚고, 이어서 Kotlin 코드가 그 문제를 어떻게 다르게
  푸는지 설명한다.
- 두 코드 사이 또는 아래에 **핵심 차이**를 한두 문장으로 요약한다.

```
### <개념 이름>

Java:
​```java
// Java 코드
​```

Kotlin:
​```kotlin
// Kotlin 코드
​```

**핵심 차이**: ...
```

## 규칙 3 — 문서 간 참조는 필요한 것만, 하단에 모아서 표시

- 문서 서두나 본문 중간에 "이 문서는 X만 다루고, Y는 다른 문서를 보라"는
  식으로 다른 문서와의 관계를 설명하지 않는다. 다른 문서를 가리키는 것이
  정말 필요하다고 판단될 때만 참조를 남기고, 그때도 설명 없이 링크만
  남긴다.
- 참조가 필요하다고 판단한 문서는 본문에 인라인 링크로 흩어놓지 않고,
  문서 하단의 **"## 관련 문서"** 절에 모아서 표시한다 (공식 문서 출처를
  적는 "## 참고 자료" 절과는 별도 절이다).
- `README.md`처럼 저장소 최상위에서 개요를 전달해야 하는 문서는, 그
  정도 분량의 내용을 다른 문서로 링크하지 않고 직접 작성한다.

---

## 패키지/문서 구조

학습 주제(Kotlin 기본 라이브러리 / Kotlin Coroutine / Spring MVC·WebFlux)별로
코드 패키지와 문서 폴더가 1:1로 대응한다.

## 관련 문서

- [README.md](./README.md) — 전체 구조와 학습 범위
